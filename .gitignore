# docs/ and .cursor/ are tracked on purpose (product spec + shared Cursor agent skills).

node_modules
dist
.vite
.env
.env.local
*.log
.DS_Store
coverage

# Playwright
playwright-report
test-results
tests/.auth

.idea
.vscode
